# App Models Folder

This folder is for application-level model definitions and schema objects.

Potential uses:
- Pydantic request/response schemas
- Domain model classes
- Shared typed payload definitions

This helps keep route files clean and strongly typed.
