# Data Folder

This folder stores datasets used by the project.

## Subfolders

- `raw/`: Input CSV files before processing (includes synthetic samples for testing).
- `processed/`: Cleaned or transformed datasets generated during workflows.
